extends layout

block content
  h1= "CU Photo Contest"
  h2 Welcome to #{title}
  p(style={color: 'red'}) #{message}

  form(action="/vote1" method="POST")
    a อันดับที่ 1
      |  
      input(type='number', name='firstPlace', value='')
      br
      br
    a อันดับที่ 2
      |  
      input(type='number', name='secondPlace', value='')
      br
      br
    a อันดับที่ 3
      |  
      input(type='number', name='thirdPlace', value='')
      br
      br
    input(type="number", name='id', value=id, style="display: none")
    button.btn.btn-primary(type='submit') Next
